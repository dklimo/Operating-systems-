# Содержание лабораторных работ

**1.** [Лабораторная работа №1: Создание процессов](#лабораторная-работа-1-создание-процессов)  
**2.** [Лабораторная работа №2: Создание потоков](#лабораторная-работа-2-создание-потоков)  
**3.** [Лабораторная работа №3: Синхронизация потоков](#лабораторная-работа-3-синхронизация-потоков-при-помощи-критических-секций-и-событий-обработка-тупиков)  
**4.** [Лабораторная работа №4: Синхронизация процессов](#лабораторная-работа-4-синхронизация-процессов)

**5.** [Лабораторная работа №5: Обмен данными](#лабораторная-работа-5-обмен-данными-по-именованным-каналам)

**6.** [Коллоквиум: Разработка REST API для управления To-Do List](#коллоквиум-разработка-rest-api-для-управления-to-do-list)


<details>
<summary> Подробное описание сборки</summary>
  
| Технология | Назначение | Версия |
|------------|------------|---------|
| **C++** | Основной язык разработки | C++20 |
| **WinAPI** | Работа с потоками и синхронизацией | - |
| **Google Test** | Юнит-тестирование | Последняя |
| **CMake** | Система сборки | 3.8+ |

</details>

---

# Лабораторная работа №1: "Создание процессов"

## Цель работы

Изучение механизмов межпроцессного взаимодействия в операционной системе Windows. Практическое применение WinAPI для создания и управления процессами, передачи данных между ними и организации файлового обмена.

## Архитектура

<details>
<summary> Подробное описание архитектуры</summary>

1. **Контроллер (Main.exe)**
   - Управляет жизненным циклом процессов
   - Обрабатывает пользовательский ввод
   - Координирует выполнение задач

2. **Генератор данных (Creator.exe)**
   - Создает структурированные данные
   - Сохраняет в бинарном формате
   - Обеспечивает целостность данных

3. **Анализатор (Reporter.exe)**
   - Обрабатывает бинарные данные
   - Выполняет вычисления
   - Форматирует отчет

</details>

## Структура проекта

```
lab1-processes/
├── 📄 main.cpp              # Основной управляющий процесс
├── 📄 creator.cpp           # Утилита создания бинарного файла
├── 📄 reporter.cpp          # Утилита генерации отчетов
├── 📄 employee.h            # Заголовочный файл структуры данных
├── 📄 employee.cpp          # Реализация работы с файлами
├── 🧪 tests.cpp             # Юнит-тесты Google Test
├── ⚙️ CMakeLists.txt        # Конфигурация сборки
└── ⚙️ CMakePresets.json     # Предустановки сборки
```

## Ключевые функции

### Main.exe
```cpp
// Создание процесса Creator
CreateProcessA(NULL, cmdLine1.data(), NULL, NULL, FALSE, 0, NULL, NULL, &si1, &pi1);

// Ожидание завершения
WaitForSingleObject(pi1.hProcess, INFINITE);

// Освобождение ресурсов
CloseHandle(pi1.hProcess);
CloseHandle(pi1.hThread);
```

## Пример работы

<details>
<summary> Показать пример выполнения</summary>

```bash
# Запуск программы
> Main.exe

Enter binary file name (.bin): employees.bin
Enter number of records: 2

┌─────────────────────────────────┐
│  Запуск Creator.exe...          │
└─────────────────────────────────┘

Employee number 1:
Enter ID: 101
Enter name: Ivanov
Enter hours worked: 160

Employee number 2:
Enter ID: 102
Enter name: Petrov
Enter hours worked: 145.5

┌─────────────────────────────────┐
│  Содержимое бинарного файла:    │
└─────────────────────────────────┘
ID: 101, Name: Ivanov, Hours: 160
ID: 102, Name: Petrov, Hours: 145.5

Enter report file name (.txt): report.txt
Enter pay per hour: 15.5

┌─────────────────────────────────┐
│  Запуск Reporter.exe...         │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│  Содержимое отчета:             │
└─────────────────────────────────┘
        ID           Name     Hours         Salary
--------------------------------------------------
       101         Ivanov     160.00        2480.00
       102         Petrov     145.50        2255.25
```

</details>

---

# Лабораторная работа №2: "Создание потоков"

## Цель работы

Изучение механизмов многопоточного программирования в операционной системе Windows. Практическое применение WinAPI для создания и управления потоками, организации параллельных вычислений и синхронизации выполнения потоков.

## Архитектура

<details>
<summary>Подробное описание архитектуры</summary>

1. **Основной поток (main)**
   - Управляет вводом данных
   - Создает и запускает рабочие потоки
   - Координирует выполнение задач
   - Выполняет финальную обработку данных

2. **Поток min_max**
   - Находит минимальный и максимальный элементы массива
   - Выводит результаты на консоль
   - Использует задержки для имитации длительных вычислений

3. **Поток average**
   - Вычисляет среднее арифметическое элементов массива
   - Выводит результат на консоль
   - Использует задержки для имитации длительных вычислений

</details>

## Структура проекта

```
lab2-threads/
├── 📄 main.cpp              # Основной поток (управляющий)
├── 📄 core.h               # Основные определения и структуры
├── 📄 core.cpp             # Реализация функций потоков
├── 📄 min_max.h            # Заголовочный файл для поиска min/max
├── 📄 min_max.cpp          # Реализация поиска min/max
├── 📄 average.h            # Заголовочный файл для вычисления среднего
├── 📄 average.cpp          # Реализация вычисления среднего
├── 🧪 tests.cpp            # Юнит-тесты Google Test
├── ⚙️ CMakeLists.txt       # Конфигурация сборки
└── ⚙️ CMakePresets.json    # Предустановки сборки
```

## Ключевые функции

### Создание потоков
```cpp
// Создание потока min_max
HANDLE hMinMax = CreateThread(NULL, 0, min_max_thread, &A, 0, &IDMinMax);

// Создание потока average  
HANDLE hAverage = CreateThread(NULL, 0, average_thread, &A, 0, &IDAverage);

// Ожидание завершения потоков
WaitForSingleObject(hMinMax, INFINITE);
WaitForSingleObject(hAverage, INFINITE);
```

### Функция потока min_max
```cpp
DWORD WINAPI min_max_thread(LPVOID lpParam) {
    vector<int>* arr = static_cast<vector<int>*>(lpParam);
    auto result = find_min_max(*arr);
    g_results.min_val = result.first;
    g_results.max_val = result.second;
    cout << "Thread min_max: min = " << g_results.min_val 
         << ", max = " << g_results.max_val << endl;
    return 0;
}
```

### Функция потока average
```cpp
DWORD WINAPI average_thread(LPVOID lpParam) {
    vector<int>* arr = static_cast<vector<int>*>(lpParam);
    g_results.average_val = find_average(*arr);
    cout << "Thread average: avg = " << g_results.average_val << endl;
    return 0;
}
```

## Пример работы

<details>
<summary>Показать пример выполнения</summary>

```bash
# Запуск программы
> Main.exe

Enter array size: 5
Enter array members: 10 20 30 40 50

┌─────────────────────────────────┐
│  Запуск потоков...              │
└─────────────────────────────────┘

Thread min_max: min = 10, max = 50
Thread average: avg = 30

┌─────────────────────────────────┐
│  Модифицированный массив:       │
└─────────────────────────────────┘
Modified array: 30 20 30 40 30
```

</details>

---

# Лабораторная работа №3: "Синхронизация потоков при помощи критических секций и событий. Обработка тупиков"

## Цель работы

Изучение механизмов синхронизации потоков в операционной системе Windows. Практическое применение критических секций и событий для управления параллельным доступом к общим ресурсам, предотвращение race conditions и организация корректного взаимодействия между потоками.

## Архитектура

<details>
<summary>Подробное описание архитектуры</summary>

1. **Основной поток (main)**
   - Управляет созданием и инициализацией массива
   - Создает и запускает потоки marker
   - Координирует работу потоков через события
   - Обрабатывает пользовательский ввод для управления потоками
   - Управляет завершением работы системы потоков

2. **Потоки marker (рабочие потоки)**
   - Параллельно работают с общим массивом
   - Помечают элементы массива своими идентификаторами
   - Используют критическую секцию для безопасного доступа к массиву
   - Синхронизируются через события с основным потоком
   - Обрабатывают сигналы продолжения/завершения работы

3. **Система синхронизации**
   - Критические секции для защиты массива
   - События для управления потоками (старт, продолжение, завершение)
   - Механизм ожидания и уведомления между потоками

</details>

## Структура проекта

```
lab3-synchronization/
├── 📄 main.cpp              # Основной поток управления
├── 📄 marker.h             # Класс управления потоками marker
├── 📄 marker.cpp           # Реализация потоков marker
├── 📄 threadsync.h         # Класс синхронизации потоков
├── 📄 threadsync.cpp       # Реализация синхронизации
├── 📄 helper.h            # Вспомогательные функции и константы
├── 📄 helper.cpp          # Реализация вспомогательных функций
├── 🧪 tests.cpp           # Юнит-тесты Google Test
├── ⚙️ CMakeLists.txt      # Конфигурация сборки
└── ⚙️ CMakePresets.json   # Предустановки сборки
```

## Ключевые функции

### Создание и управление потоками
```cpp
// Создание потоков marker
for (int i = 0; i < threadCount; i++) {
    ThreadParams* params = new ThreadParams{ this, i + 1 };
    threads[i] = CreateThread(NULL, 0, MarkerThread, params, 0, NULL);
}

// Ожидание сигнала на старт
WaitForSingleObject(sync.GetStartEvent(), INFINITE);
```

### Синхронизация с использованием критических секций
```cpp
// Безопасный доступ к массиву
sync.EnterCriticalSection();
if (array[index] == 0) {
    Sleep(THREAD_SLEEP);
    array[index] = threadId;
    markedIndices.push_back(index);
    marksCount++;
    Sleep(THREAD_SLEEP);
}
sync.LeaveCriticalSection();
```

### Управление событиями
```cpp
// Ожидание сигналов продолжения или завершения
HANDLE hEvents[2] = {
    sync.GetContinueEvent(),
    sync.GetTerminateEvent(threadIndex)
};

DWORD dwEvent = WaitForMultipleObjects(2, hEvents, FALSE, INFINITE);

if (dwEvent == WAIT_OBJECT_0 + 1) {
    working = false; // Сигнал завершения
}
```

## Пример работы

<details>
<summary>Показать пример выполнения</summary>

```bash
# Запуск программы
> Marker.exe

Enter array size: 10
Enter number of marker threads: 3

┌─────────────────────────────────┐
│  Запуск потоков marker...       │
└─────────────────────────────────┘

Thread 1: marked 5 elements, cannot mark index 3
Thread 2: marked 4 elements, cannot mark index 7
Thread 3: marked 6 elements, cannot mark index 2

┌─────────────────────────────────┐
│  Все потоки ожидают             │
└─────────────────────────────────┘

Array: 1, 2, 3, 1, 2, 0, 3, 2, 1, 3

Active streams: 1 2 3

Please enter the stream number to complete (1-3): 2

After the thread completes 2:
Array: 1, 0, 3, 1, 0, 0, 3, 0, 1, 3

┌─────────────────────────────────┐
│  Продолжение работы потоков...  │
└─────────────────────────────────┘

Thread 1: marked 8 elements, cannot mark index 5
Thread 3: marked 9 elements, cannot mark index 6

All threads have completed their work.
Main thread exiting.
```

</details>

## Особенности реализации

### Цветной вывод
- Каждый поток имеет свой цвет для удобства отслеживания
- Использование ANSI escape-кодов для терминалов, поддерживающих цвет

### Принудительное ожидание
- Потоки периодически (каждые 10 пометок) переходят в состояние ожидания
- Позволяет основному потоку получать контроль над системой

### Валидация ввода
- Проверка корректности размера массива
- Проверка количества потоков
- Проверка выбора потока для завершения

---

# Лабораторная работа №4: "Синхронизация процессов"

## Цель работы

Изучение механизмов межпроцессного взаимодействия через разделяемые ресурсы в операционной системе Windows. Практическое применение мьютексов, семафоров и событий для организации безопасной передачи данных между процессами через общий файл с использованием кольцевой очереди (FIFO).

## Архитектура

<details>
<summary>Подробное описание архитектуры</summary>

1. **Процесс Receiver (приемник)**
   - Создает и управляет общим бинарным файлом
   - Инициализирует систему синхронизации (мьютекс, семафоры)
   - Запускает и координирует процессы Sender
   - Читает сообщения из кольцевой очереди
   - Управляет жизненным циклом всей системы

2. **Процессы Sender (отправители)**
   - Подключаются к общему файлу
   - Отправляют сигнал готовности Receiver
   - Записывают сообщения в кольцевую очередь
   - Синхронизируют доступ к файлу через общие объекты
   - Обрабатывают пользовательские команды

3. **Система синхронизации**
   - **Мьютекс** - для исключительного доступа к файлу
   - **Семафор empty** - отслеживает свободные слоты в буфере
   - **Семафор full** - отслеживает заполненные слоты в буфере
   - **События ready** - сигнализируют о готовности процессов Sender

4. **Кольцевая очередь FIFO**
   - Циклический буфер в общем файле
   - Поддержка порядка сообщений "первым пришел - первым вышел"
   - Автоматическая обработка переполнения

</details>

## Структура проекта

```
lab4-process-sync/
├── 📄 receiver.cpp          # Основной процесс-приемник
├── 📄 sender.cpp            # Процессы-отправители
├── 📄 process.h             # Функции управления процессами
├── 📄 process.cpp           # Реализация функций процессов
├── 📄 utils.h               # Вспомогательные утилиты и константы
├── 📄 utils.cpp             # Реализация утилит
├── 🧪 tests.cpp             # Юнит-тесты Google Test
├── ⚙️ CMakeLists.txt        # Конфигурация сборки
└── ⚙️ CMakePresets.json     # Предустановки сборки
```

## Ключевые функции

### Создание системы синхронизации (Receiver)
```cpp
// Создание объектов синхронизации
hMutex = CreateMutexA(NULL, FALSE, mutexName.c_str());
hEmptySemaphore = CreateSemaphoreA(NULL, message_count, message_count, emptySemName.c_str());
hFullSemaphore = CreateSemaphoreA(NULL, 0, message_count, fullSemName.c_str());
```

### Запись сообщения (Sender)
```cpp
// Безопасная запись в кольцевую очередь
DWORD result = WaitForSingleObject(hEmptySemaphore, WAIT_TIMEOUT_MS);
WaitForSingleObject(hMutex, INFINITE);

file.seekp(index * MAX_MESSAGE_LENGTH);
file.write(message.c_str(), MAX_MESSAGE_LENGTH);

ReleaseMutex(hMutex);
ReleaseSemaphore(hFullSemaphore, 1, NULL);
```

### Чтение сообщения (Receiver)
```cpp
// Безопасное чтение из кольцевой очереди
DWORD result = WaitForSingleObject(hFullSemaphore, WAIT_TIMEOUT_MS);
WaitForSingleObject(hMutex, INFINITE);

file.seekg(read_index * MAX_MESSAGE_LENGTH);
file.read(message, MAX_MESSAGE_LENGTH);

ReleaseMutex(hMutex);
ReleaseSemaphore(hEmptySemaphore, 1, NULL);
```

### Запуск процессов Sender
```cpp
// Запуск нескольких процессов-отправителей
for (int i = 0; i < sender_count; i++) {
    string command = "sender.exe \"" + filename + "\" \"" +
        eventName + "\" \"" + mutexName + "\" \"" +
        emptySemName + "\" \"" + fullSemName + "\" \"" +
        to_string(message_count) + "\"";
    
    launchSenderProcess(command, processes[i]);
}
```

## Пример работы

<details>
<summary>Показать пример выполнения</summary>

```bash
# Запуск приемника
> receiver.exe

Binary filename: messages.bin
Buffer size (messages): 5
Number of senders: 3

┌─────────────────────────────────┐
│  Создание системы...            │
└─────────────────────────────────┘

File created: messages.bin (buffer: 5 messages)
Sender 1 started
Sender 2 started
Sender 3 started

Waiting for 3 senders...
All senders ready!

┌─────────────────────────────────┐
│  Режим приема сообщений         │
└─────────────────────────────────┘

Command (read/exit): read
Message: Hello from sender 1

Command (read/exit): read
Message: Test message 2

Command (read/exit): exit

Receiver finished

# В консолях отправителей:
> sender.exe "messages.bin" "messages_ready_1" ...

Sender ready (buffer: 5)

Command (send/exit): send
Message (20 chars max): Hello from sender 1
Sent to slot 0

Command (send/exit): exit
Sender exiting...
```

</details>

## Особенности реализации

### Цветной интерфейс
- Разные цвета для различных типов сообщений (ошибки, успех, предупреждения)
- Улучшенная читаемость в многопроцессной среде

### Кольцевая очередь FIFO
- Циклический буфер фиксированного размера
- Автоматическое управление индексами чтения/записи
- Поддержка нескольких отправителей с сохранением порядка

### Механизмы синхронизации
- **Мьютекс** - защищает доступ к файлу от одновременной записи/чтения
- **Семафор empty** - считает свободные слоты (инициализируется размером буфера)
- **Семафор full** - считает заполненные слоты (инициализируется 0)
- **События ready** - координируют запуск системы

### Обработка ошибок
- Проверка создания всех объектов синхронизации
- Обработка таймаутов при ожидании семафоров
- Защита от утечек ресурсов (закрытие хэндлов в деструкторах)

---
На основе предоставленного кода для **Лабораторной работы №5** и предыдущих описаний, вот полное описание пятой лабораторной работы в том же стиле, включая все необходимые файлы (включая недостающий `utils.h` и `tests.cpp`):

---

## **Лабораторная работа №5: "Обмен данными по именованным каналам"**

### **Цель работы**
Изучение механизмов межпроцессного взаимодействия через именованные каналы в Windows. Практическое применение WinAPI для реализации клиент-серверной архитектуры с поддержкой конкурентного доступа к общим данным, использованием блокировок записей (read/write locks) и синхронизацией через критические секции.

---

### **Архитектура**

#### **Структура проекта**
```
lab5-client-server/
├── 📄 client.h             # Заголовочный файл клиента
├── 📄 client.cpp           # Реализация клиента
├── 📄 server.h             # Заголовочный файл сервера
├── 📄 server.cpp           # Реализация сервера
├── 📄 main.cpp             # Точка входа сервера
├── 📄 employee.h           # Структуры данных Employee, Request, Response
├── 📄 record_lock.h        # Механизм блокировок записей
├── 📄 utils.h              # Вспомогательные функции и константы
├── 🧪 tests.cpp            # Юнит-тесты Google Test
├── ⚙️ CMakeLists.txt       # Конфигурация сборки
└── ⚙️ CMakePresets.json    # Предустановки сборки
```

---

### **Ключевые компоненты**

#### **1. Сервер (`server.cpp`, `server.h`)**
- **Создание бинарного файла** сотрудников
- **Управление именованными каналами** (Named Pipes) для каждого клиента
- **Реализация read/write блокировок** записей:
  - **Read lock**: несколько клиентов могут читать одновременно
  - **Write lock**: эксклюзивный доступ для записи
- **Обработка клиентских запросов** в отдельных потоках
- **Синхронизация** через `CRITICAL_SECTION` и мьютексы

#### **2. Клиент (`client.cpp`, `client.h`)**
- **Подключение к серверу** через именованный канал
- **Меню взаимодействия**:
  - **Модификация записи** (требует write lock)
  - **Чтение записи** (требует read lock)
- **Автоматическое освобождение блокировок** при завершении
- **Цветной интерактивный интерфейс**

#### **3. Механизм блокировок (`record_lock.h`)**
```cpp
struct RecordLock {
    CRITICAL_SECTION cs;
    int readers;    // количество активных читателей
    bool writer;    // флаг активного писателя
    
    void lock_read();   // блокировка для чтения
    void unlock_read(); // разблокировка чтения
    void lock_write();  // блокировка для записи
    void unlock_write();// разблокировка записи
};
```

#### **4. Структуры данных (`employee.h`)**
```cpp
struct Employee {
    int id;
    char name[10];
    double hours;
};

enum class OperationType {
    READ = 1,
    WRITE = 2,
    END = 3
};

struct Request {
    OperationType operationType;
    int employeeId;
    Employee emp;
};

struct Response {
    bool success;
    Employee emp;
};
```

---

### **Протокол взаимодействия**

#### **Типы запросов:**
1. **READ** — запрос на чтение записи
2. **WRITE** — запрос на запись (с данными или без)
3. **END** — завершение работы с записью (освобождение блокировки)

#### **Последовательность модификации записи:**
```
Клиент -> Сервер: WRITE (без данных) - запрос блокировки
Сервер -> Клиент: Response (успех/ошибка + текущие данные)
Клиент: Показывает текущие данные, запрашивает изменения
Клиент -> Сервер: WRITE (с измененными данными)
Сервер: Обновляет запись, отправляет подтверждение
Клиент -> Сервер: END - освобождает блокировку
```

#### **Последовательность чтения записи:**
```
Клиент -> Сервер: READ - запрос на чтение
Сервер -> Клиент: Response (данные или ошибка)
Клиент: Отображает данные
Клиент -> Сервер: END - освобождает read lock
```

---

### **Ключевые функции**

#### **Создание именованного канала (Сервер):**
```cpp
HANDLE hPipe = CreateNamedPipeA(
    pipeName.c_str(),
    PIPE_ACCESS_DUPLEX,
    PIPE_TYPE_MESSAGE | PIPE_READMODE_MESSAGE | PIPE_WAIT,
    MAX_PIPE_INSTANCES,
    PIPE_BUFFER_SIZE,
    PIPE_BUFFER_SIZE,
    PIPE_TIMEOUT,
    NULL
);
```

#### **Подключение клиента к каналу:**
```cpp
hPipe = CreateFileA(
    pipeName.c_str(),
    GENERIC_READ | GENERIC_WRITE,
    0,
    NULL,
    OPEN_EXISTING,
    0,
    NULL
);
```

#### **Обработка клиента в отдельном потоке:**
```cpp
void Server::handleClient(int client_id, HANDLE hPipe) {
    while (running) {
        Request request;
        ReadFile(hPipe, &request, sizeof(Request), &bytesRead, NULL);
        
        Response response{};
        switch (request.operationType) {
            case OperationType::READ:
                // Обработка запроса на чтение
                break;
            case OperationType::WRITE:
                // Обработка запроса на запись
                break;
            case OperationType::END:
                // Освобождение блокировки
                break;
        }
        WriteFile(hPipe, &response, sizeof(Response), &bytesWritten, NULL);
    }
}
```

---

### **Пример выполнения**

```
=== SERVER STARTED ===
============================================================

=== CREATE BINARY FILE ===
Input name of binary file: employees.bin
Input number of employees (max 100): 3

Employee #1:
Employee's id (1-100): 1
Employee's name (max 9 chars): John
Work hours: 40

Employee #2:
...

[SUCCESS] File created successfully with 3 employees

============================================================
FILE CONTENTS:
------------------------------------------------------------
Employee #1:
Employee's id: 1
Employee's name: John
Work hours: 40
...

Input count of client processes (max 255): 2

[INFO] Created pipe: \\.\pipe\lab5_pipe_0
[INFO] Created pipe: \\.\pipe\lab5_pipe_1
[INFO] Starting client 0...
[INFO] Starting client 1...

=== CLIENT 0 STARTED ===
========================================
=== CLIENT 0 MENU ===
1. Modify record
2. Read record
3. Exit
Your choice: 1

=== MODIFY RECORD ===
Enter employee ID to modify: 1
[INFO] Client 0 locked record 1 for writing
------------------------------------------------------------
CURRENT EMPLOYEE DATA:
Employee's id: 1
Employee's name: John
Work hours: 40
------------------------------------------------------------
ENTER NEW DATA (enter - to keep current):
New name (max 9 chars, current: "John"): Jonathan
New work hours (current: 40): 45
------------------------------------------------------------
Enter 'send' to send modified record to server (or 'cancel' to abort): send
[SUCCESS] Record updated successfully!
------------------------------------------------------------
Enter 'finish' to release lock (this is required): finish
[INFO] Lock released successfully.

=== CLIENT 1 MENU ===
1. Modify record
2. Read record
3. Exit
Your choice: 2

=== READ RECORD ===
Enter employee ID to read: 1
[INFO] Client 1 locked record 1 for reading
------------------------------------------------------------
EMPLOYEE DATA:
Employee's id: 1
Employee's name: Jonathan
Work hours: 45
------------------------------------------------------------
Enter 'finish' to release read lock (this is required): finish
[INFO] Read lock released successfully.
```

---

### **Особенности реализации**

1. **Многоуровневая синхронизация**:
   - Критические секции для защиты структур данных
   - Read/write блокировки на уровне записей
   - Мьютексы для управления клиентскими блокировками

2. **Отказоустойчивость**:
   - Автоматическое переподключение клиентов
   - Обработка разрывов соединения
   - Гарантированное освобождение ресурсов

3. **Пользовательский интерфейс**:
   - Цветной вывод для разных типов сообщений
   - Интерактивные подсказки
   - Валидация ввода

4. **Масштабируемость**:
   - Поддержка до 255 клиентов
   - Динамическое создание каналов
   - Потоковая обработка запросов

---

# **Коллоквиум: Разработка REST API для управления To-Do List**

## **Общее описание проекта**

### **Цель проекта**
Разработка отказоустойчивого, масштабируемого REST API для управления списком задач (To-Do List) с полной реализацией CRUD операций, включая современные практики DevOps и мониторинга.

---

## ** Основные требования**

### **1. Базовый функционал (REST API)**
- **CRUD операции** для ресурса `Task`:
  - **Create** (POST) - создание задачи
  - **Read** (GET) - получение списка задач и отдельной задачи
  - **Update** (PUT/PATCH) - полное/частичное обновление
  - **Delete** (DELETE) - удаление задачи
- **Стандартные HTTP методы и коды состояния**:
  - 200 OK, 201 Created, 404 Not Found, 500 Internal Server Error
- **Формат обмена данными**: JSON
- **Структура задачи (Task)**:
  ```json
  {
    "id": 1,
    "title": "Купить молоко",
    "description": "Обязательно 3.2% жирности",
    "status": "todo",
    "created_at": "2024-01-15T10:30:00Z",
    "updated_at": "2024-01-15T10:30:00Z"
  }
  ```

### **2. Расширенный функционал (дополнительные требования)**

| № | Компонент | Описание | Реализация в проекте |
|---|-----------|----------|---------------------|
| **1** | **Работа с БД** | Постоянное хранение данных | SQLite (для простоты), SQLAlchemy ORM |
| **2** | **API Gateway** | Балансировка, rate limiting, auth | FastAPI middleware, CORS, валидация |
| **3** | **Кеширование** | Ускорение чтения данных | Redis + TTL кэширование (5 минут) |
| **4** | **Очередь сообщений** | Асинхронная обработка | Redis как брокер сообщений |
| **5** | **Логирование и метрики** | Мониторинг и отладка | Structlog + Prometheus метрики |
| **6** | **Отказоустойчивость** | Надежность на всех уровнях | Docker Compose, health checks, рестарты |
| **7** | **Автоматическое развертывание** | DevOps практики | Docker контейнеризация |

---

## ** Архитектура системы**


### **Технологический стек**

| Компонент | Технология | Назначение |
|-----------|------------|------------|
| **Backend Framework** | FastAPI 0.104.1 | Высокопроизводительный async REST API |
| **ORM** | SQLAlchemy 2.0 | Работа с реляционной БД |
| **База данных** | SQLite | Постоянное хранение задач |
| **Кэширование** | Redis 7 | In-memory кэш для ускорения запросов |
| **Валидация** | Pydantic 2.5 | Валидация и сериализация данных |
| **Конфигурация** | Pydantic Settings 2.1 | Управление настройками |
| **Логирование** | Structlog 23.2 | Структурированное логирование |
| **Метрики** | Prometheus Client 0.19 | Сбор метрик производительности |
| **Контейнеризация** | Docker + Docker Compose | Развертывание и оркестрация |
| **Мониторинг** | Prometheus + Grafana | Визуализация метрик и мониторинг |

---

## ** Эндпоинты API**

### **Основные эндпоинты**

| Метод | Эндпоинт | Описание | Коды ответа |
|-------|----------|----------|-------------|
| **GET** | `/` | Корневой эндпоинт, информация об API | 200 |
| **GET** | `/health` | Проверка здоровья сервиса | 200 |
| **GET** | `/tasks` | Получить список всех задач | 200 |
| **POST** | `/tasks` | Создать новую задачу | 201 |
| **GET** | `/tasks/{id}` | Получить задачу по ID | 200, 404 |
| **PUT** | `/tasks/{id}` | Полностью обновить задачу | 200, 404 |
| **PATCH** | `/tasks/{id}` | Частично обновить задачу | 200, 404 |
| **DELETE** | `/tasks/{id}` | Удалить задачу | 204, 404 |
| **GET** | `/metrics` | Prometheus метрики | 200 |

### **Подробное описание эндпоинтов**

#### **1. GET /tasks - Получить список задач**
```http
GET /tasks?skip=0&limit=100&search=молоко HTTP/1.1
Host: localhost:8000
```
**Параметры запроса:**
- `skip`: количество пропускаемых записей (по умолчанию 0)
- `limit`: максимальное количество записей (по умолчанию 100)
- `search`: поиск по названию или описанию

**Ответ (200 OK):**
```json
[
  {
    "id": 1,
    "title": "Купить молоко",
    "description": "Обязательно 3.2% жирности",
    "status": "todo",
    "created_at": "2024-01-15T10:30:00Z",
    "updated_at": "2024-01-15T10:30:00Z"
  }
]
```

#### **2. POST /tasks - Создать задачу**
```http
POST /tasks HTTP/1.1
Content-Type: application/json

{
  "title": "Купить молоко",
  "description": "Обязательно 3.2% жирности",
  "status": "todo"
}
```

**Ответ (201 Created):**
```json
{
  "id": 1,
  "title": "Купить молоко",
  "description": "Обязательно 3.2% жирности",
  "status": "todo",
  "created_at": "2024-01-15T10:30:00Z",
  "updated_at": null
}
```

#### **3. GET /tasks/{id} - Получить задачу по ID**
```http
GET /tasks/1 HTTP/1.1
```

**Ответ (200 OK):**
```json
{
  "id": 1,
  "title": "Купить молоко",
  "description": "Обязательно 3.2% жирности",
  "status": "todo",
  "created_at": "2024-01-15T10:30:00Z",
  "updated_at": null
}
```

**Ошибка (404 Not Found):**
```json
{
  "detail": "Задача не найдена"
}
```

#### **4. PUT /tasks/{id} - Полное обновление**
```http
PUT /tasks/1 HTTP/1.1
Content-Type: application/json

{
  "title": "Купить молоко и хлеб",
  "description": "Обязательно 3.2% жирности",
  "status": "in_progress"
}
```

#### **5. PATCH /tasks/{id} - Частичное обновление**
```http
PATCH /tasks/2 HTTP/1.1
Content-Type: application/json

{
  "status": "done"
}
```

---

## **Мониторинг и логирование**

### **Структурированное логирование (Structlog)**
```python
# Пример логирования
logger.info(
    "task_created",
    task_id=db_task.id,
    title=db_task.title,
    status=db_task.status
)
```

**Формат логов:**
```json
{
  "event": "task_created",
  "task_id": 1,
  "title": "Купить молоко",
  "status": "todo",
  "timestamp": "2024-01-15T10:30:00Z",
  "level": "info"
}
```

### **Метрики Prometheus**
```python
# Счетчик HTTP запросов
REQUESTS = Counter('http_requests_total', 'Total HTTP Requests', 
                   ['method', 'endpoint', 'status'])

# Гистограмма времени выполнения
REQUEST_DURATION = Histogram('http_request_duration_seconds', 
                           'HTTP request duration in seconds',
                           ['method', 'endpoint'])
```

**Доступные метрики:**
- `http_requests_total` - общее количество HTTP запросов
- `http_request_duration_seconds` - время выполнения запросов
- Доступно по адресу: `GET /metrics`

---

## ** Развертывание с Docker Compose**

### **Архитектура развертывания**

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   FastAPI API   │────│     Redis       │────│    SQLite DB    │
│   (localhost:8000) │    │   (Кэширование)   │    │   (Хранение)     │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         └───────────────────────┼───────────────────────┘
                                 │
                     ┌─────────────────┐
                     │   Prometheus    │────┐
                     │   (Метрики)     │    │
                     └─────────────────┘    │
                                 │          │
                     ┌─────────────────┐    │
                     │     Grafana     │◄───┘
                     │   (Визуализация)│
                     └─────────────────┘
```

### **Доступ к сервисам**

| Сервис | URL | Назначение | Порт |
|--------|-----|------------|------|
| **FastAPI** | http://localhost:8000 | Основное API | 8000 |
| **FastAPI Docs** | http://localhost:8000/docs | Swagger документация | 8000 |
| **Redis** | redis://localhost:6379 | Кэширование | 6379 |
| **Prometheus** | http://localhost:9090 | Сбор метрик | 9090 |
| **Grafana** | http://localhost:3000 | Визуализация | 3000 |

---

## **Что было реализовано в проекте**

### ** Программа минимум:**
1. **REST API с полным CRUD**
2. **Валидация данных** (Pydantic)
3. **Стандартные HTTP коды**
4. **JSON формат обмена**

### ** Дополнительные улучшения:**
1. **Работа с БД** - SQLite + SQLAlchemy ORM
2. **API Gateway функционал** - Middleware, CORS, Rate limiting (базовый)
3. **Кэширование** - Redis с TTL 5 минут
4. **Очередь сообщений** - Redis как брокер (готов к интеграции с Celery)
5. **Логирование и метрики** - Structlog + Prometheus + Grafana
6. **Отказоустойчивость** - Docker Compose, health checks, рестарты
7. **Автоматическое развертывание** - Docker контейнеризация

---


## Автор

**Студент 2 курса БГУ ФПМИ**  
📧 Email: *dasha.klim2006@gmail.com*  
💻 GitHub: [dklimo](https://github.com/dklimo)  

---

<div align="center">

*Лабораторная работа выполнена в рамках курса "Операционные системы"*  
*Белорусский государственный университет, ФПМИ*  
*2025-2026 учебный год*

![БГУ](https://img.shields.io/badge/БГУ-ФПМИ-blue?style=for-the-badge)

</div>

---
